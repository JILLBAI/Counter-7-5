// 當頁面準備完成時
$(document).ready(function(){

    // 當視窗捲動時
    $(window)..scroll(function () { 
        
        // var 變數=資料
        var var offset = $(selector).offset();
        var top = offset.top;
        var left = offset.left;
        
    });
}